node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
.env*.local

# Runtime store is generated by `npm run scan` / server actions from fixtures.
# Keep the directory tracked (so a fresh checkout has somewhere to write) but
# ignore the generated JSON files themselves.
/data/store/*.json
!/data/store/.gitkeep
